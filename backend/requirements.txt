# =====================================================
# 跌倒检测与预警系统 - 后端依赖清单
# 开发/测试环境: Python 3.10.7
# 安装方式:  pip install -r requirements.txt
# =====================================================

# ---------- Web 框架 ----------
flask>=3.1.3
flask-cors>=6.0.2
# Flask 的依赖，但代码中直接使用了 werkzeug.utils.secure_filename
werkzeug>=3.1.3

# ---------- 计算机视觉 / 深度学习 ----------
# ultralytics 会通过依赖自动安装 torch / torchvision / numpy / pillow / pyyaml 等
ultralytics>=8.4.33
opencv-python>=4.13.0

# 如需显式控制 PyTorch 版本（默认从 PyPI 安装 CPU 版），取消注释并按需调整：
# torch>=2.5.0
# torchvision>=0.20.0

# ---------- 数据库 (MongoDB) ----------
# gridfs 模块包含在 pymongo 中，无需单独安装
pymongo>=4.16.0

# ---------- 认证令牌 ----------
itsdangerous>=2.2.0

# ---------- DeepSeek AI 分析 ----------
# 需在 .env 中配置 DEEPSEEK_API_KEY，未配置时功能自动降级
openai>=2.30.0

# ---------- 可选：阿里云短信 SDK ----------
# 仅当使用报警短信通知时才需要（代码中为延迟导入，缺失不影响系统启动）
# 安装后还需在报警配置中填写 AccessKey / 签名 / 模板
# alibabacloud-dysmsapi20170525>=2.0.0
# alibabacloud-tea-openapi>=0.3.0
